const PageTitle = ({ title }) => {
  return (
    <div className="text-center mt-[30px]">
      <h2 className="text-color-8 text-[32px] font-bold">{title}</h2>
    </div>
  );
};

export default PageTitle;
